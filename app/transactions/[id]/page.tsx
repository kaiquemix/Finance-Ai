const Transaction = ({ params: { id } }) => {
  return (
    <div className="">
      <h1>{id}</h1>
    </div>
  );
};

export default Transaction;

//Usa o nome da pasta [ID] que recebe o ID que condiz com o nome da pasta
